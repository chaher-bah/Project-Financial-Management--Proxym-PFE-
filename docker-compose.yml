services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - ELASTICSEARCH_URL=http://elasticsearch:9200
      - KEYCLOAK_URL=http://keycloak:8080
      - CLIENT_SECRET=TlzbPPRDSOFU6feYQL3PsMGt7gJXQpmX
      - KEYCLOAK_REALM=Proxym-IT
      - KEYCLOAK_CLIENT_ID=pfe
    depends_on:
      - keycloak
    mem_limit: 2g
    volumes:
      - ./backend:/app
      - /app/node_modules

  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.12.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data

  kibana:
    image: docker.elastic.co/kibana/kibana:8.12.0
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    depends_on:
      - elasticsearch

  keycloak:
    image: quay.io/keycloak/keycloak:latest
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=admin
      - DB_VENDOR=h2
      - KC_CACHE_THEMES=false   
      - KC_CACHE_TEMPLATES=false
      - KEYCLOAK_IMPORT=/opt/keycloak/data/import/realm.json
    ports:
      - "8080:8080"
    command:
      - start-dev
      - --import-realm
    volumes:
      - ./myKeycloakTheme/myTheme:/opt/keycloak/themes/myTheme
      - ./myKeycloakTheme/realmSettings/realm.json:/opt/keycloak/data/import/realm.json

volumes:
  mongodb_data:
  elasticsearch_data: 